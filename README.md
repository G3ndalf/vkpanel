# VK IP Panel

Панель управления скриптами ловли floating IP в VK Cloud.

![Python](https://img.shields.io/badge/python-3.11+-blue)
![FastAPI](https://img.shields.io/badge/FastAPI-0.109-green)

## Возможности

- 📊 Дашборд со статусами всех скриптов в реальном времени
- ▶️ Управление скриптами: Start / Stop / Restart
- ☁️ Просмотр floating IP из VK Cloud
- 🔄 Автоматическое обновление статусов
- 🔐 Авторизация

## Архитектура

```
┌─────────────────┐     SSH      ┌─────────────────┐
│  VK IP Panel    │─────────────▶│   VPS серверы   │
│  (FastAPI)      │              │  (vk-fip@*)     │
└─────────────────┘              └─────────────────┘
        │                                │
        │                                ▼
        │                        ┌─────────────────┐
        │                        │   VK Cloud API  │
        │                        │  (floating IPs) │
        └───────────────────────▶└─────────────────┘
```

## Установка

### 1. Клонировать репозиторий

```bash
git clone https://github.com/G3ndalf/vkpanel.git
cd vkpanel
```

### 2. Создать виртуальное окружение

```bash
python -m venv venv
source venv/bin/activate  # Linux/macOS
# или
venv\Scripts\activate     # Windows
```

### 3. Установить зависимости

```bash
pip install -r requirements.txt
```

### 4. Настроить переменные окружения

```bash
cp .env.example .env
# Отредактировать .env
```

### 5. Создать data.json

```bash
# Вариант 1: Скопировать пример
cp data.example.json /opt/vkpanel/data.json

# Вариант 2: Сканировать серверы автоматически
export SSH_PASS=your_password
python scan_servers.py
cp data.json /opt/vkpanel/data.json
```

### 6. Запустить

```bash
# Разработка
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000

# Продакшен
uvicorn app.main:app --host 0.0.0.0 --port 8000 --workers 4
```

## Docker

```bash
# Собрать образ
docker build -t vkpanel .

# Запустить
docker run -d \
  -p 8000:8000 \
  -v /opt/vkpanel:/opt/vkpanel \
  -e ADMIN_PASS=your_password \
  -e SECRET_KEY=your_secret \
  --name vkpanel \
  vkpanel
```

## Systemd

```bash
# Скопировать service файл
sudo cp vkpanel.service /etc/systemd/system/

# Включить и запустить
sudo systemctl daemon-reload
sudo systemctl enable vkpanel
sudo systemctl start vkpanel
```

## Структура проекта

```
vkpanel/
├── app/
│   ├── __init__.py
│   ├── main.py         # FastAPI приложение
│   ├── config.py       # Конфигурация
│   ├── models.py       # Pydantic модели
│   ├── data.py         # Работа с данными
│   ├── ssh.py          # SSH функции
│   └── openstack.py    # OpenStack API
├── templates/          # Jinja2 шаблоны
├── static/             # Статические файлы
├── data.json           # Данные (не в git!)
├── requirements.txt
├── Dockerfile
└── README.md
```

## API

| Endpoint | Метод | Описание |
|----------|-------|----------|
| `/` | GET | Дашборд |
| `/servers` | GET | Список серверов |
| `/cloud` | GET | Floating IPs |
| `/api/refresh` | POST | Обновить статусы |
| `/api/status` | GET | Получить кэш статусов |
| `/api/cloud/refresh` | POST | Обновить cloud данные |

## Скрипты

- `scan_servers.py` — автоматическое сканирование серверов
- `add_projects.py` — добавление OpenStack проектов

## Безопасность

⚠️ **Важно:**
- `data.json` содержит пароли — добавлен в `.gitignore`
- Используйте `.env` для секретов
- Меняйте `SECRET_KEY` на продакшене
- Используйте HTTPS через nginx/reverse proxy

## Лицензия

MIT
